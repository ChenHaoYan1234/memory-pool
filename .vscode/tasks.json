{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "gcc -g ${workspaceFolder}/main.c ${workspaceFolder}/mem.c -I${workspaceFolder}/includes -o main"
        }
    ]
}